import styles from '@/styles/Top.module.css';
import LoginTitle from './LoginTitle';

function Top() {
  return (
    <div className={styles.top}>
      <LoginTitle />
    </div>
  );
}

export default Top;
